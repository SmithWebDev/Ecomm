<h1>Welcome to Tech Talent Store</h1>
<p>Give us your money!</p>
<div class="row">
  <% @products.each do |product| %>
    <div class="col-md-4">
      <div class="well">
        <% if product.image.url.nil? == false %>
          <p><%= image_tag product.image.url %></p>
        <% end %>
        <p><a href="#"><span class="glyphicon glyphicon-plus"></span>Quick Info</a></p>
        <h3><%= link_to  product.name, product %></h3>
        <p><%= number_to_currency product.price %></p><br><br>
        <p>Made by: <%= product.brand %></p>
      </div>
    </div>
  <% end %>
</div>